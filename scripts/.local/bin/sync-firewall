#!/usr/bin/env bash

# Elevate permissions
sudo -v

SETUP_FILE="$HOME/dotfiles/projects/Projects/setup-script/lib/setup-firewall"

if [[ -f "$SETUP_FILE" ]]; then
    chmod u+x "$SETUP_FILE"
    bash "$SETUP_FILE"
else
    echo "Error: $SETUP_FILE not found!"
    exit 1
fi
